*** Settings ***
Resource          ../../../../../resource/测试ERP/信贷模块/客户管理/[2] 准客户页面/var准客户页面.txt

*** Variables ***
# ${excel_path}    客户信息表格.xls    #批量造单子表格
# ${element}      //div[text()='选择文件' and @name!='qqfile']
# ${element}      //div[text()='选择文件']
${element}        //div[@style='position: relative; overflow: hidden; direction: ltr;']

*** Test Cases ***
测试一下上传文件
    [Documentation]    批量造单子，数据驱动，填写姓名，号码，身份证，即可.这里结束测试不做关闭浏览器操作
    [Tags]    数据库    批量造单子
    [Setup]    连接ERP数据库
    [Template]
    登录到准客户页面
    查找准客户    自动化会被删除
    sleep    2
    选中准客户
    sleep    2
    Click Element    //a[text()='编辑资料']
    sleep    2
    点击上传素材
    sleep    2
    点击上传身份证
    sleep    2
    log    1
    # @{elements}    Get Webelements    ${element}
    # ${Attribute}    Get Elementset    Attribute    ${element}
    # ${count}    Get Matching Xpath Count    ${element}
    # log    ${count}
    # log many    @{elements}
    # log    ${Attribute}
    Log Location
    #Mouse Over    ${element}
    # @{elements}    Get Webelements    ${element}
    # ${Attribute1}    Get Element Attribute    ${element}
    # log many    @{elements}
    # log    ${Attribute1}
    sleep    2
    上传文件
      Log Location
    [Teardown]    断开数据库    #run keywords | Close Browser | 断开数据库 | #Close Browser | # 关闭浏览器

elements
    ${var}    Set Variable    123
    @{list}    Create List    a    b    c
    log    ${var}
    Log Many    @{list}
    ${list1}    Set Variable    abc
    log    ${var}
    log    ${list1[0:2]}
    log    ${list1[0:3]}
